name: Script
description: A file written in a scripting language that serves as an entry point, launcher, or automation utility within a @Software.
extends: @DesignPattern
properties:
  - name: interpreter
    description: the interpreter used to run this script (e.g. bash, python, sh)
