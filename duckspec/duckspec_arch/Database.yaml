description: A persistent data store with a defined schema. Stores and retrieves records, documents, or key-value pairs in response to queries.
extends: @DesignPattern
properties:
  - id: engine
    description: the database engine (e.g. PostgreSQL, MySQL, SQLite, MongoDB, Redis)
  - id: schema
    description: reference to the @Schema that defines the structure of stored data
