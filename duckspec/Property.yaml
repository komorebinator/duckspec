description: A named field on a @Term that holds a value.
extends: @Term
properties:
  - id: type
    description: free-form description of the value type
  - id: default
    description: default value if not explicitly set
