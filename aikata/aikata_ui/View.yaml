name: View
description: A container within a @UserInterface that groups related @UserInterfaceElement instances — a window, screen, page, or any other full UI surface.
extends: @UserInterfaceElement
contains:
  - name: elements
    description: list of @UserInterfaceElement the @View contains
